# Copia este archivo a .env.local (no se sube a git) y complétalo tras ejecutar:
#   npx convex dev
# El comando pedirá iniciar sesión en convex.dev, crear/vincular un proyecto,
# y escribirá estos valores automáticamente en .env.local.

CONVEX_DEPLOYMENT=
NEXT_PUBLIC_CONVEX_URL=
NEXT_PUBLIC_CONVEX_SITE_URL=
